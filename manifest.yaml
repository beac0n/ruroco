# see https://github.com/rust-mobile/xbuild/issues/125

icon: "icon.png"

android:
  manifest:
    package: "org.beac0n.ruroco"
    application:
      label: "Ruroco"
    uses_permission:
      - name: android.permission.INTERNET
      - name: android.permission.ACCESS_NETWORK_STATE